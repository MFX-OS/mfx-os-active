{
  "name": "mfx-os",
  "version": "3.0.0",
  "private": true,
  "description": "MFX OS — Microflex Film Corporation Manufacturing Operations System",
  "scripts": {
    "build": "node build.js",
    "build:dev": "node build.js --dev",
    "deploy": "node build.js && firebase deploy --only hosting:os",
    "deploy:all": "node build.js && firebase deploy",
    "clean": "node build.js --clean",
    "audit": "npm audit --omit=dev && cd functions && npm audit --omit=dev",
    "audit:fix": "npm audit fix && cd functions && npm audit fix",
    "test:smoke": "node scripts/smoke-test-endpoints.js",
    "wipe:quotes": "node scripts/wipe-fresh-start.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
