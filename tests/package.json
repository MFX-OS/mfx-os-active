{
  "name": "mfx-os-tests",
  "private": true,
  "description": "Firestore rules + future integration tests for MFX OS",
  "scripts": {
    "test": "node --test rules.test.js",
    "test:rules": "node --test rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0"
  }
}
